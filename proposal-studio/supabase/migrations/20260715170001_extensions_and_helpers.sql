-- Extensiones y funciones auxiliares compartidas por el resto de las migraciones.

create extension if not exists vector;

create or replace function public.set_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.updated_at = now();
  return new;
end;
$$;
